#include "main.h"

/**
 * a function that prints the alphabet, in lowercase, followed by a new line.
 */
 
void print_alphabet(void)
{
    for (char c = 'a'; c <= 'z'; c++)
    {
       _putchar(c);
    }
   _putchar('\n');
}
