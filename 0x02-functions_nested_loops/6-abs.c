#include "main.h"
/**
 * _abs - computes absolute value of integer
 * @k :input number as integer
 * Return: absolute value
 */

int _abs(int k)
{
if (k >= 0)
{
return (k);
}
else
{
return (k * -1);
}
}
